apiVersion: v1
kind: Service
metadata:
  name: rag-pipeline
  namespace: rag-pipeline
spec:
  selector:
    app: rag-pipeline
  ports:
    - port: 8080
      targetPort: http
      protocol: TCP
      name: http
  type: ClusterIP
